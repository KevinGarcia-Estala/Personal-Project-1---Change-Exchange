public interface Interface{
 

    /*
     * second method
     * will split apart the change into hundreds/greater than hundreds, tens, ones, and coins/decimal number
     */
    void splitAndCount(String Change);

    //userInput
    void userInput();
}